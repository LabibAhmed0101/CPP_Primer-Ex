Exercise 3.9: 
What does the following program do? Is it valid? If not, why not?

string s;
cout << s[0] << endl;

This program  starts an empty string variable s , then prints the 1st element (char) of s
into the screen.

its invalid program, because s is empty string and such behavior is undefined .
