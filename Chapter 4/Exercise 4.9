Exercise 4.9: 
Explain the behavior of the condition in the following if:

const char *cp = "Hello World";
if (cp && *cp)

cp = 'H' so its non zero value so it will be evaluted to true.
*cp is not a null pointer so it will be evaluted to true.

then the whole if condition is true.
